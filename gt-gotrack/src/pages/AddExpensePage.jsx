/**
 * @file AddExpensePage.jsx
 * @description Add expense form page — assembled in Phase 3.
 */

import { PageWrapper } from '../components/layout/PageWrapper';

/** @returns {JSX.Element} Add expense page placeholder */
export default function AddExpensePage() {
  return (
    <PageWrapper>
      <div className="bg-white rounded-2xl p-8 shadow-sm border border-gray-100 text-gray-500 text-sm">
        Add Expense form — coming in Phase 3
      </div>
    </PageWrapper>
  );
}
